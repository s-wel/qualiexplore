version: '3'

services:

  qualiexplore:
    ports:
      - 80:80
    
    image: swel/qualiexplore:i4q
  
  gql-mongo:
    ports:
      - "5000:5000"

    env_file:
      - .env

    image: hrabhijith/gql-mongo:quali